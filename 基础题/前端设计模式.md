# 前端设计模式

设计模式是在软件设计过程中针对特定问题的典型解决方案。在前端开发中，合理运用设计模式可以提高代码的可维护性、可扩展性和复用性。

## 📚 设计模式分类

### 1. 创建型模式 (Creational Patterns)

#### 🏭 单例模式 (Singleton Pattern)
**定义**: 确保一个类只有一个实例，并提供全局访问点。

**应用场景**:
- 全局状态管理
- 数据库连接池
- 缓存管理

**代码示例**:
```javascript
// ES6 单例实现
class Singleton {
  static getInstance() {
    if (!Singleton.instance) {
      Singleton.instance = new Singleton();
    }
    return Singleton.instance;
  }
  
  constructor() {
    if (Singleton.instance) {
      throw new Error('单例模式禁止直接调用构造函数');
    }
  }
}

// 使用
const instance1 = Singleton.getInstance();
const instance2 = Singleton.getInstance();
console.log(instance1 === instance2); // true

// Vue Vuex store 实际应用
const store = new Vuex.Store({
  state: { count: 0 },
  mutations: {
    increment(state) { state.count++ }
  }
});
```

#### 🏗️ 工厂模式 (Factory Pattern)
**定义**: 创建对象的接口，让子类决定实例化哪个类。

**应用场景**:
- 组件创建
- API 请求封装
- 不同环境的配置

**代码示例**:
```javascript
// 简单工厂模式
class Button {
  constructor(type) {
    this.type = type;
  }
  
  render() {
    console.log(`渲染 ${this.type} 按钮`);
  }
}

class ButtonFactory {
  static createButton(type) {
    switch(type) {
      case 'primary':
        return new Button('主要');
      case 'secondary':
        return new Button('次要');
      case 'danger':
        return new Button('危险');
      default:
        return new Button('默认');
    }
  }
}

// React 组件工厂
const ComponentFactory = {
  createInput: (props) => <input {...props} />,
  createButton: (props) => <button {...props} />,
  createSelect: (props) => <select {...props} />
};
```

#### 🎨 建造者模式 (Builder Pattern)
**定义**: 将复杂对象的构建分离，使同样的构建过程可以创建不同表示。

**应用场景**:
- 复杂表单构建
- DOM 元素创建
- 配置对象构建

**代码示例**:
```javascript
class FormBuilder {
  constructor() {
    this.fields = [];
  }
  
  addField(type, name, label) {
    this.fields.push({ type, name, label });
    return this;
  }
  
  addValidation(name, rules) {
    const field = this.fields.find(f => f.name === name);
    if (field) {
      field.validation = rules;
    }
    return this;
  }
  
  build() {
    return {
      fields: this.fields,
      render: () => this.renderForm()
    };
  }
  
  renderForm() {
    return this.fields.map(field => {
      // 渲染逻辑
      return `<${field.type} name="${field.name}">`;
    }).join('');
  }
}

// 使用
const form = new FormBuilder()
  .addField('input', 'username', '用户名')
  .addValidation('username', ['required', 'minLength:3'])
  .addField('input', 'email', '邮箱')
  .addValidation('email', ['required', 'email'])
  .build();
```

---

### 2. 结构型模式 (Structural Patterns)

#### 🎯 适配器模式 (Adapter Pattern)
**定义**: 将一个类的接口转换成客户希望的另一个接口。

**应用场景**:
- 第三方库接口适配
- 数据格式转换
- API 版本兼容

**代码示例**:
```javascript
// 适配不同的地图 API
class GoogleMaps {
  showMap() {
    console.log('显示 Google 地图');
  }
}

class BaiduMaps {
  displayMap() {
    console.log('显示百度地图');
  }
}

// 适配器
class MapsAdapter {
  constructor(mapService) {
    this.mapService = mapService;
  }
  
  showMap() {
    if (this.mapService.displayMap) {
      this.mapService.displayMap();
    } else {
      this.mapService.showMap();
    }
  }
}

// 数据格式适配器
class LegacyAPIAdapter {
  constructor(oldAPI) {
    this.api = oldAPI;
  }
  
  getUsers() {
    const oldData = this.api.fetchUsers();
    return oldData.map(user => ({
      id: user.user_id,
      name: user.user_name,
      email: user.email_address
    }));
  }
}
```

#### 🔗 装饰器模式 (Decorator Pattern)
**定义**: 动态地给对象添加一些额外的职责。

**应用场景**:
- 组件功能增强
- 请求拦截器
- 日志记录

**代码示例**:
```javascript
// React 高阶组件 (HOC) 就是装饰器模式的应用
function withAuth(WrappedComponent) {
  return function AuthenticatedComponent(props) {
    const [isAuthenticated, setIsAuthenticated] = useState(false);
    
    useEffect(() => {
      // 检查认证状态
      const token = localStorage.getItem('token');
      setIsAuthenticated(!!token);
    }, []);
    
    if (!isAuthenticated) {
      return <div>请先登录</div>;
    }
    
    return <WrappedComponent {...props} />;
  };
}

// 使用
const ProtectedProfile = withAuth(ProfileComponent);

// 方法装饰器
function log(target, propertyKey, descriptor) {
  const originalMethod = descriptor.value;
  
  descriptor.value = function(...args) {
    console.log(`调用 ${propertyKey}，参数:`, args);
    const result = originalMethod.apply(this, args);
    console.log(`${propertyKey} 返回:`, result);
    return result;
  };
  
  return descriptor;
}

class UserService {
  @log
  getUser(id) {
    return { id, name: 'John' };
  }
}
```

#### 🎪 外观模式 (Facade Pattern)
**定义**: 为子系统中的一组接口提供一个一致的界面。

**应用场景**:
- API 封装
- 复杂操作简化
- 第三方服务集成

**代码示例**:
```javascript
// 复杂的 DOM 操作封装
class DOMFacade {
  createElement(tag, attributes = {}, children = []) {
    const element = document.createElement(tag);
    
    // 设置属性
    Object.entries(attributes).forEach(([key, value]) => {
      element.setAttribute(key, value);
    });
    
    // 添加子元素
    children.forEach(child => {
      if (typeof child === 'string') {
        element.appendChild(document.createTextNode(child));
      } else {
        element.appendChild(child);
      }
    });
    
    return element;
  }
  
  createForm(fields) {
    const formElement = this.createElement('form');
    
    fields.forEach(field => {
      const label = this.createElement('label', {}, [field.label]);
      const input = this.createElement('input', {
        type: field.type,
        name: field.name,
        placeholder: field.placeholder
      });
      
      formElement.appendChild(label);
      formElement.appendChild(input);
    });
    
    return formElement;
  }
}

// HTTP 请求封装
class HTTPFacade {
  async get(url, config = {}) {
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Content-Type': 'application/json',
        ...config.headers
      },
      ...config
    });
    
    return this.handleResponse(response);
  }
  
  async post(url, data, config = {}) {
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        ...config.headers
      },
      body: JSON.stringify(data),
      ...config
    });
    
    return this.handleResponse(response);
  }
  
  async handleResponse(response) {
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    return await response.json();
  }
}
```

---

### 3. 行为型模式 (Behavioral Patterns)

#### 👀 观察者模式 (Observer Pattern)
**定义**: 定义对象间的一种一对多的依赖关系。

**应用场景**:
- 事件处理
- 状态管理 (Redux, Vuex)
- 响应式数据

**代码示例**:
```javascript
// 简单的观察者模式实现
class Subject {
  constructor() {
    this.observers = [];
  }
  
  subscribe(observer) {
    this.observers.push(observer);
  }
  
  unsubscribe(observer) {
    this.observers = this.observers.filter(obs => obs !== observer);
  }
  
  notify(data) {
    this.observers.forEach(observer => observer.update(data));
  }
}

class Observer {
  constructor(name) {
    this.name = name;
  }
  
  update(data) {
    console.log(`${this.name} 收到更新:`, data);
  }
}

// 使用
const subject = new Subject();
const observer1 = new Observer('观察者1');
const observer2 = new Observer('观察者2');

subject.subscribe(observer1);
subject.subscribe(observer2);
subject.notify('状态更新');

// React useEffect 实际应用
function useCustomListener(eventName, callback) {
  useEffect(() => {
    window.addEventListener(eventName, callback);
    return () => {
      window.removeEventListener(eventName, callback);
    };
  }, [eventName, callback]);
}
```

#### 🎭 发布-订阅模式 (Publisher-Subscriber Pattern)
**定义**: 在发布者和订阅者之间增加一个调度中心。

**应用场景**:
- 消息队列
- 事件总线
- 组件通信

**代码示例**:
```javascript
class EventEmitter {
  constructor() {
    this.events = {};
  }
  
  on(event, callback) {
    if (!this.events[event]) {
      this.events[event] = [];
    }
    this.events[event].push(callback);
  }
  
  off(event, callback) {
    if (this.events[event]) {
      this.events[event] = this.events[event].filter(cb => cb !== callback);
    }
  }
  
  emit(event, data) {
    if (this.events[event]) {
      this.events[event].forEach(callback => callback(data));
    }
  }
}

// 全局事件总线
const eventBus = new EventEmitter();

// 组件 A 发布事件
function ComponentA() {
  const handleClick = () => {
    eventBus.emit('userAction', { type: 'click', timestamp: Date.now() });
  };
  
  return <button onClick={handleClick}>点击</button>;
}

// 组件 B 订阅事件
function ComponentB() {
  const [actions, setActions] = useState([]);
  
  useEffect(() => {
    const handleUserAction = (data) => {
      setActions(prev => [...prev, data]);
    };
    
    eventBus.on('userAction', handleUserAction);
    
    return () => {
      eventBus.off('userAction', handleUserAction);
    };
  }, []);
  
  return <div>操作记录: {actions.length}</div>;
}
```

#### 🤝 策略模式 (Strategy Pattern)
**定义**: 定义一系列算法，把它们封装起来，并且使它们可相互替换。

**应用场景**:
- 表单验证
- 动画效果
- 排序算法

**代码示例**:
```javascript
// 表单验证策略
const validationStrategies = {
  required: (value) => {
    if (!value || value.trim() === '') {
      return '此字段为必填项';
    }
    return null;
  },
  
  email: (value) => {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(value)) {
      return '请输入有效的邮箱地址';
    }
    return null;
  },
  
  minLength: (value, min) => {
    if (value.length < min) {
      return `最少需要 ${min} 个字符`;
    }
    return null;
  }
};

class Validator {
  constructor() {
    this.strategies = validationStrategies;
  }
  
  validate(value, rules) {
    for (const rule of rules) {
      const [strategyName, ...params] = rule.split(':');
      const error = this.strategies[strategyName](value, ...params);
      if (error) return error;
    }
    return null;
  }
}

// 动画策略
const animationStrategies = {
  fadeIn: (element, duration) => {
    element.style.opacity = 0;
    element.style.transition = `opacity ${duration}ms`;
    setTimeout(() => element.style.opacity = 1, 0);
  },
  
  slideIn: (element, duration) => {
    element.style.transform = 'translateX(-100%)';
    element.style.transition = `transform ${duration}ms`;
    setTimeout(() => element.style.transform = 'translateX(0)', 0);
  }
};

class Animator {
  constructor(strategy) {
    this.strategy = strategy;
  }
  
  setStrategy(strategy) {
    this.strategy = strategy;
  }
  
  animate(element, duration = 300) {
    this.strategy(element, duration);
  }
}
```

#### 🔄 状态模式 (State Pattern)
**定义**: 允许一个对象在其内部状态改变时改变它的行为。

**应用场景**:
- 订单状态管理
- 游戏角色状态
- UI 组件状态

**代码示例**:
```javascript
// 订单状态管理
class OrderContext {
  constructor() {
    this.state = new PendingState();
    this.order = { items: [], total: 0 };
  }
  
  setState(state) {
    this.state = state;
    this.state.setContext(this);
  }
  
  addItem(item) {
    this.state.addItem(item);
  }
  
  pay() {
    this.state.pay();
  }
  
  ship() {
    this.state.ship();
  }
}

class PendingState {
  addItem(item) {
    this.context.order.items.push(item);
    this.context.order.total += item.price;
  }
  
  pay() {
    console.log('支付成功');
    this.context.setState(new PaidState());
  }
  
  ship() {
    console.log('订单未支付，无法发货');
  }
}

class PaidState {
  addItem() {
    console.log('已支付订单无法添加商品');
  }
  
  pay() {
    console.log('订单已支付');
  }
  
  ship() {
    console.log('发货成功');
    this.context.setState(new ShippedState());
  }
}

// React 组件状态模式
function OrderComponent() {
  const [orderState, setOrderState] = useState('pending');
  
  const actions = {
    pending: {
      canAddItem: true,
      canPay: true,
      canShip: false
    },
    paid: {
      canAddItem: false,
      canPay: false,
      canShip: true
    },
    shipped: {
      canAddItem: false,
      canPay: false,
      canShip: false
    }
  };
  
  const currentActions = actions[orderState];
  
  return (
    <div>
      <button 
        disabled={!currentActions.canPay}
        onClick={() => setOrderState('paid')}
      >
        支付
      </button>
      <button 
        disabled={!currentActions.canShip}
        onClick={() => setOrderState('shipped')}
      >
        发货
      </button>
    </div>
  );
}
```

#### 🎯 模板方法模式 (Template Method Pattern)
**定义**: 定义一个操作中的算法骨架，而将一些步骤延迟到子类中。

**应用场景**:
- 组件生命周期
- 数据处理流程
- 表单提交流程

**代码示例**:
```javascript
// 抽象类定义模板
class DataProcessor {
  process() {
    this.loadData();
    this.validateData();
    this.transformData();
    this.saveData();
  }
  
  loadData() {
    throw new Error('子类必须实现 loadData 方法');
  }
  
  validateData() {
    console.log('默认数据验证');
  }
  
  transformData() {
    console.log('默认数据转换');
  }
  
  saveData() {
    throw new Error('子类必须实现 saveData 方法');
  }
}

// 具体实现
class UserDataProcessor extends DataProcessor {
  loadData() {
    console.log('从 API 加载用户数据');
    this.data = { name: 'John', age: 30 };
  }
  
  validateData() {
    console.log('验证用户数据格式');
  }
  
  saveData() {
    console.log('保存用户数据到数据库');
  }
}

// React 模板方法模式
class BaseComponent extends React.Component {
  componentDidMount() {
    this.init();
  }
  
  init() {
    this.setupData();
    this.setupEventListeners();
    this.renderContent();
  }
  
  setupData() {
    // 默认数据设置
  }
  
  setupEventListeners() {
    // 默认事件监听
  }
  
  renderContent() {
    // 默认渲染逻辑
  }
}

class UserComponent extends BaseComponent {
  setupData() {
    this.setState({ users: [] });
  }
  
  renderContent() {
    return <div>用户组件内容</div>;
  }
}
```

---

## 🔄 现代前端框架中的设计模式

### React 中的设计模式

#### 1. 高阶组件 (HOC) - 装饰器模式
```javascript
function withLoading(WrappedComponent) {
  return function WithLoadingComponent({ isLoading, ...props }) {
    if (isLoading) {
      return <div>Loading...</div>;
    }
    return <WrappedComponent {...props} />;
  };
}
```

#### 2. Render Props - 策略模式
```javascript
class MouseTracker extends React.Component {
  state = { x: 0, y: 0 };
  
  handleMouseMove = (event) => {
    this.setState({
      x: event.clientX,
      y: event.clientY
    });
  };
  
  render() {
    return (
      <div onMouseMove={this.handleMouseMove}>
        {this.props.render(this.state)}
      </div>
    );
  }
}

// 使用
<MouseTracker render={({ x, y }) => (
  <h1>鼠标位置: ({x}, {y})</h1>
)} />
```

#### 3. 自定义 Hook - 模板方法模式
```javascript
function useApi(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  
  useEffect(() => {
    const fetchData = async () => {
      try {
        setLoading(true);
        const response = await fetch(url);
        const result = await response.json();
        setData(result);
      } catch (err) {
        setError(err);
      } finally {
        setLoading(false);
      }
    };
    
    fetchData();
  }, [url]);
  
  return { data, loading, error };
}
```

### Vue 中的设计模式

#### 1. Mixin - 模板方法模式
```javascript
const commonMixin = {
  created() {
    this.init();
  },
  
  methods: {
    init() {
      this.setupData();
    },
    
    setupData() {
      // 默认设置
    }
  }
};
```

#### 2. 插件模式 - 单例模式
```javascript
const MyPlugin = {
  install(Vue, options) {
    Vue.prototype.$myMethod = function() {
      // 全局方法
    };
    
    Vue.directive('my-directive', {
      // 全局指令
    });
  }
};
```

### 设计模式选择指南

| 场景 | 推荐模式 | 应用示例 |
|------|----------|----------|
| 创建复杂对象 | 工厂模式、建造者模式 | 表单生成器、组件工厂 |
| 需要唯一实例 | 单例模式 | 全局状态管理、缓存 |
| 接口不兼容 | 适配器模式 | 第三方库集成 |
| 动态添加功能 | 装饰器模式 | 权限控制、日志记录 |
| 简化复杂接口 | 外观模式 | API 封装、工具库 |
| 一对多依赖 | 观察者模式 | 事件处理、状态同步 |
| 算法切换 | 策略模式 | 表单验证、动画效果 |
| 状态变化复杂 | 状态模式 | 订单流程、游戏角色 |
| 固定流程算法 | 模板方法模式 | 数据处理、组件生命周期 |

## 📝 总结

设计模式不是银弹，过度使用会增加系统复杂性。在实际开发中应该：

1. **理解场景**: 明确问题的本质和约束
2. **适度使用**: 不要为了用模式而用模式
3. **保持简单**: 优先选择最简单的解决方案
4. **团队协作**: 选择团队熟悉的模式
5. **持续重构**: 根据需求变化调整设计

合理运用设计模式可以让代码更加优雅、可维护，为项目长期发展奠定良好基础。
